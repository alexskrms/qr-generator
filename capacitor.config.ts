import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.reteed.qrgenerator',
  appName: 'Qr Generator',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
